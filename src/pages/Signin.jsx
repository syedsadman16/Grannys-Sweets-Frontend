import React from "react";
import LoginForm from "../components/LoginForm";

const Signin = (props) => {
  return (
    <div>
      <h1>Sign in page</h1>
      <LoginForm />
    </div>
  );
};

export default Signin;
